<%= form_for ([@post, @post.comments.build]) do |f| %>

	<%= f.label :name %><br>
	<%= f.text_field :name %><br>
	<br>
	<%= f.label :comment%><br>
	<%= f.text_field :comment %><br>
	<br>
	<%= f.submit %>
<% end %>
